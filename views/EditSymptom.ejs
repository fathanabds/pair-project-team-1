<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit Patient Symptom</title>
  </head>
  <body>
    <div class="flex flex-col w-full max-w-md mx-auto min-h-full p-10">
      <h1 class="text-lg font-semibold text-gray-900">
        Edit Patient Symptom Medical Record #<%= patientSymptoms.id %>
      </h1>
      <form class="mt-10 flex flex-col gap-y-8" method="post">
        <div>
          <label class="block text-sm font-medium" for="symptom">Symptom</label>
          <br />
          <textarea
            class="block rounded-md border border-gray-300 text-gray-900 w-full"
            name="symptom"
            id="symptom"
          >
  <%= patientSymptoms.symptom %></textarea
          >
        </div>
        <div class="flex w-full">
          <button
            class="rounded-md text-base font-semibold shadow-sm bg-blue-600 hover:bg-gray-500 text-white w-full"
          >
            Submit
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
